rules:
  github-app:
    ignore:
      - renovate.yaml:30:11
      - renovate.yaml:120:11
  secrets-outside-env:
    config:
      allow:
        - APP_CLIENT_ID
        - PRIVATE_KEY
        - DOCKERHUB_USERNAME
        - DOCKERHUB_TOKEN
